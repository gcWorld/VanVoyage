org.gradle.jvmargs=-Xmx4G
android.useAndroidX=true
android.enableJetifier=true
android.defaults.buildfeatures.buildconfig=true
android.nonTransitiveRClass=false

# Flutter SDK properties for plugins
flutter.compileSdkVersion=35
flutter.targetSdkVersion=34
flutter.minSdkVersion=21
flutter.ndkVersion=25.1.8937393
flutter.buildToolsVersion=34.0.0

#Mapbox
# The MAPBOX_DOWNLOADS_TOKEN can be set via:
# 1. Environment variable (recommended for local dev): export MAPBOX_DOWNLOADS_TOKEN=sk.your_token
# 2. This file (less secure): MAPBOX_DOWNLOADS_TOKEN=sk.your_token
# 3. android/local.properties (gitignored, most secure for local dev)
# The build.gradle will read from these sources in order: local.properties, environment, gradle.properties
# Leave this commented out and use environment variables or local.properties instead
# MAPBOX_DOWNLOADS_TOKEN=
